<form method="post" action="{{ path('app_user_delete_profile', {'id': user.iduser}) }}" onsubmit="return confirm('Are you sure you want to delete your account?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.iduser) }}">
    <button class="btn">Delete Account</button>
</form>
